import { Server, Socket } from 'socket.io';
import { PlayerRole, MatchPhase, DoorState, Vector3Data, NetworkPlayer } from 'shared';
export declare class GameRoom {
    code: string;
    phase: MatchPhase;
    timer: number;
    players: Map<string, NetworkPlayer>;
    doorStates: Record<string, DoorState>;
    hidingSpots: Record<string, string | null>;
    adminId: string;
    private activeInteractions;
    private extractionProgress;
    private matchStats;
    private matchStartTime;
    private matchEndTimer;
    private gameLoopInterval;
    private pulseCooldowns;
    private noiseEvents;
    private noiseCooldowns;
    private io;
    constructor(code: string, io: Server);
    private resetRoom;
    addPlayer(id: string, displayName: string, socket: Socket): void;
    removePlayer(id: string): void;
    setPlayerReady(id: string, isReady: boolean): void;
    setPlayerPreference(id: string, preferredRole: string): void;
    startMatch(requestingPlayerId: string): void;
    private transitionToPhase;
    private gameTick;
    private processExtraction;
    private processInteractions;
    private cancelInteraction;
    private completeInteraction;
    handlePlayerMove(playerId: string, pos: Vector3Data, rotY: number, currentSpeedMode: 'walk' | 'sprint' | 'crouch', dt: number): void;
    private addNoiseEvent;
    handleStartInteraction(playerId: string, type: 'capture' | 'revive' | 'search' | 'lock' | 'break' | 'breach', targetId: string): void;
    handleCancelInteraction(playerId: string): void;
    handleDoorToggle(playerId: string, doorId: string): void;
    handleHidingSpotInteract(playerId: string, spotId: string): void;
    handlePulseAttack(playerId: string, direction: Vector3Data): void;
    skipPhase(playerId: string): void;
    addTime(playerId: string, seconds: number): void;
    forceReleaseRole(playerId: string, role: PlayerRole): void;
    resetDoors(playerId: string): void;
    teleportPlayer(playerId: string, pos: Vector3Data): void;
    private checkWinConditions;
    private endMatch;
    private broadcastState;
    private processBots;
    private getNavigationTarget;
    private updateCivilianBot;
    private updateAttackerBot;
    private updateRescueBot;
    private moveBotTowards;
    private handleNearbyClosedDoors;
    private handleNearbyLockedDoors;
}
